# Twichu Web

```
cd server && npm start
```

## Install & Build

```
npm install
cd client && npm install
cd server && npm install
npm start
```

## Install MongoDB

```
npm install -g mongodb
mongod
```
