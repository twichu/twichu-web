# demo-web

```
npm start
```

## Express <- Vue.js

```
npm install
cd client && npm install
cd server && npm install
cd client && npm run build
cd server && npm start
```

## MongoDB

```
npm install -g mongodb
mongod
```
